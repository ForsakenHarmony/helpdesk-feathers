sudo: false

language: node_js

node_js:
  - "6"

cache:
  yarn: true
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run webpack:prod
  - npm run test
  - ./node_modules/coveralls/bin/coveralls.js < ./coverage/lcov.info
